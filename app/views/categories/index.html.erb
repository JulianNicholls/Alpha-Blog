<div class="page-header">
  <h1>Current categories</h1>
</div>

<% @categories.each do |category| %>
  <ul class="listing">
    <div class="row">
      <div class="well col-md-4 col-md-offset-4">
        <li class="article-title text-center">
          <%= link_to category.name, category %>
        </li>
      </div>
    </div>
  </ul>
<% end %>
